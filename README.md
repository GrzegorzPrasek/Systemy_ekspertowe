# System ekspertowy do doboru kierunku studiów

Projekt systemu ekspertowego wspomagającego wybór kierunku studiów na podstawie profilu użytkownika.

## Struktura projektu
- `src/` – kod źródłowy (Prolog)
- `data/` – przykładowe dane, scenariusze
- `doc/` – dokumentacja
